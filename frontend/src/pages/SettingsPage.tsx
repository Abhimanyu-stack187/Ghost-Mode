import { Settings2 } from "lucide-react";
import { PlaceholderPage } from "../components/ui/PlaceholderPage";

export function SettingsPage() {
  return <PlaceholderPage eyebrow="Application Controls" title="Settings" description="Telemetry connection, appearance intensity, and demo controls will live here." icon={Settings2} />;
}
