import { Eye } from "lucide-react";
import { PlaceholderPage } from "../components/ui/PlaceholderPage";

export function PermissionsPage() {
  return <PlaceholderPage eyebrow="Sensitive Access" title="Permissions" description="Camera, microphone, and clipboard observations will be explored here." icon={Eye} />;
}
