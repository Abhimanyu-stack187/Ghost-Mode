import { RadioTower } from "lucide-react";
import { PlaceholderPage } from "../components/ui/PlaceholderPage";

export function ExposureMapPage() {
  return <PlaceholderPage eyebrow="Outbound Activity" title="Exposure Map" description="Animated app-to-domain-to-tracker flows and exposure heatmaps will live here." icon={RadioTower} />;
}
