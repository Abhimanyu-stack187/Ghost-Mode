import { RotateCcw } from "lucide-react";
import { PlaceholderPage } from "../components/ui/PlaceholderPage";

export function PrivacyReplayPage() {
  return <PlaceholderPage eyebrow="Timeline Reconstruction" title="Privacy Replay" description="Replay controls, timeline scrubbing, and progressive telemetry reveals will live here." icon={RotateCcw} />;
}
