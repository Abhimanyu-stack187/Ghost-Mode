import { KeyRound } from "lucide-react";
import { PlaceholderPage } from "../components/ui/PlaceholderPage";

export function LeaksPage() {
  return <PlaceholderPage eyebrow="Historical Exposure" title="Leaks" description="Known breach records and exposed service context will be presented here." icon={KeyRound} />;
}
