import { GitFork } from "lucide-react";
import { PlaceholderPage } from "../components/ui/PlaceholderPage";

export function DigitalShadowPage() {
  return <PlaceholderPage eyebrow="Relationship Graph" title="Digital Shadow" description="The full D3 tracker relationship canvas will land in the next visualization pass." icon={GitFork} />;
}
